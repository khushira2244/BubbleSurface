import {Icon,type IconName} from "./icons";import styles from "./landing.module.css";
const human=["Uses the normal security application","Reviews important context","Approves, rejects or modifies consequential actions","Sees execution and verification results"];const agent=["Discovers tools through WebMCP","Investigates using structured capabilities","Prepares and proposes actions","Executes only when the capability becomes valid"];
export function HumanAgentSection(){return <section className={styles.section} aria-labelledby="actors-title"><p className={styles.sectionLabel}>One shared workflow</p><h2 id="actors-title">How humans and agents use it</h2><div className={styles.peopleGrid}><PeopleCard icon="human" title="Human" items={human}/><PeopleCard icon="agent" title="Agent" items={agent}/></div><p className={styles.sharedState}><Icon name="surface" size={22}/> Both act against the same live page, state and workflow.</p></section>}
function PeopleCard({icon,title,items}:{icon:IconName;title:string;items:string[]}){return <article className={styles.peopleCard}><span className={styles.personIcon}><Icon name={icon} size={34}/></span><div><h3>{title}</h3><ul>{items.map(item=><li key={item}><Icon name="check" size={17}/>{item}</li>)}</ul></div></article>}
