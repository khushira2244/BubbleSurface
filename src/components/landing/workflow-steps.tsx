import {Icon,type IconName} from "./icons";import styles from "./landing.module.css";
const steps:Array<[IconName,string]>=[["code","Security application loads BubbleSurface"],["surface","BubbleSurface derives current WebMCP capabilities"],["agent","Agent discovers investigation tools"],["proposal","Agent investigates and prepares a consequential action"],["human","Human reviews the exact proposal"],["lock","BubbleSurface exposes the exact approved execution capability"],["execute","Agent executes; the server revalidates authority"],["verify","Execution disappears, verification appears, and the result is verified"]];
export function WorkflowSteps(){return <section className={`${styles.section} ${styles.workflow}`} id="how-it-works" aria-labelledby="workflow-title"><p className={styles.sectionLabel}>A dynamic surface</p><h2 id="workflow-title">How BubbleSurface works</h2><p className={styles.workflowLead}>Investigate <span>→</span> Propose <span>→</span> Approve <span>→</span> Execute <span>→</span> Verify</p><ol className={styles.stepGrid}>{steps.map(([icon,text],index)=><li key={text}><b>{index+1}</b><span className={styles.stepIcon}><Icon name={icon}/></span><p>{text}</p></li>)}</ol></section>}
